/* WallKeep visual scanner v12 - precomputed index */
(function(){
let model=null,index=null,vBusy=false,vLocked=false,missing=0,seen=0,lastDet=null;
const norm=s=>String(s||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const l2=v=>{const n=Math.sqrt(v.reduce((s,x)=>s+x*x,0))||1;return v.map(x=>x/n)};
const cos=(a,b)=>{let s=0;const n=Math.min(a.length,b.length);for(let i=0;i<n;i++)s+=a[i]*b[i];return s};
const same=(a,b)=>{a=norm(a);b=norm(b);return !!a&&!!b&&(a===b||a.includes(b)||b.includes(a))};
function lastCard(){return current()?.cards?.[0]||null}
async function loadModel(){if(model)return model;$('#scanStatus').textContent='Carico modello visuale…';await tf.ready();model=await mobilenet.load({version:2,alpha:0.5});return model}
async function loadIndex(){if(index)return index;$('#scanStatus').textContent='Carico indice OP16…';const r=await fetch('data/op16-index.json?v=12',{cache:'no-store'});if(!r.ok)throw Error('Indice OP16 non ancora disponibile');const d=await r.json();if(d.model!=='mobilenet-v2-alpha-0.5'||!Array.isArray(d.cards)||!d.cards.length)throw Error('Indice OP16 non valido');index=d.cards;return index}
async function embedding(el){const m=await loadModel();const t=m.infer(el,true);const a=l2(Array.from(await t.data()));t.dispose();return a}
function addReview(card,confidence){const l=current();l.game=game;l.name=$('#scanListName').value||l.name;l.cards.unshift({id:crypto.randomUUID?crypto.randomUUID():Date.now()+''+Math.random(),game:'One Piece',quantity:1,condition:'',language:'',foil:'',price:'',resolved:false,needsReview:true,visionConfidence:confidence,...card});save();navigator.vibrate?.([50,30,50])}
function duplicate(card,confidence){document.querySelector('#duplicateBtn')?.remove();const b=document.createElement('button');b.id='duplicateBtn';b.className='btn w100';b.style.marginTop='8px';b.textContent='+ AGGIUNGI UN’ALTRA COPIA DI '+card.name.toUpperCase();b.onclick=()=>{const x=lastCard();if(x&&same(x.name,card.name)){x.quantity=(+x.quantity||1)+1;save()}else addReview(card,confidence);b.remove();$('#scanStatus').innerHTML='<span class=good>✓ Copia aggiunta. Rimuovi la carta.</span>'};$('#scanStatus').insertAdjacentElement('afterend',b);$('#scanStatus').innerHTML='<span class=bad>'+esc(card.name)+' è uguale alla precedente. Conferma solo se è una seconda copia.</span>'}
async function ocrText(canvas){try{const w=await ocr();return (await w.recognize(canvas)).data.text||''}catch(e){return''}}
function boost(c,text){const t=norm(text),name=norm(c.name),code=norm(c.collector);let b=0;if(name&&t.includes(name))b+=.22;for(const w of name.split(' '))if(w.length>3&&t.includes(w))b+=.04;if(code&&t.includes(code))b+=.35;if(t.includes('op16'))b+=.05;return Math.min(.4,b)}
function showDebug(rows,text){let el=document.querySelector('#ocrDebug');if(!el){el=document.createElement('details');el.id='ocrDebug';el.className='panel';el.innerHTML='<summary style="cursor:pointer;color:#f3d17a;font-weight:700">Diagnostica riconoscimento</summary><pre id="ocrDebugText" style="white-space:pre-wrap;font-size:11px;color:#aaa"></pre>';$('#scanStatus').insertAdjacentElement('afterend',el)}el.querySelector('#ocrDebugText').textContent='OCR:\n'+text+'\n\nTOP CANDIDATES:\n'+rows.slice(0,5).map((x,i)=>`${i+1}. ${x.name} ${x.collector} • visual ${x._visual.toFixed(3)} • bonus ${x._boost.toFixed(3)} • score ${x._score.toFixed(3)}`).join('\n')}
async function identify(canvas){const idx=await loadIndex();const [q,text]=await Promise.all([embedding(canvas),ocrText(canvas)]);const rows=idx.map(c=>{const visual=cos(q,c.vec),b=boost(c,text);return{...c,_visual:visual,_boost:b,_score:visual*.86+b}}).sort((a,b)=>b._score-a._score);showDebug(rows,text);return rows}
scanDetected=async function(d){if(vBusy||vLocked)return;vBusy=true;try{if(game!=='onepiece')throw Error('Scanner visuale v12 in test solo su One Piece OP16');const frozen=warpDetected(d);$('#scanStatus').textContent='Fotogramma congelato • confronto con OP16…';const rows=await identify(frozen);if(!rows.length)throw Error('Nessun candidato');const card=rows[0],clean={...card};delete clean.vec;delete clean._visual;delete clean._boost;delete clean._score;vLocked=true;const last=lastCard();if(last&&same(last.name,clean.name)){duplicate(clean,card._score);return}addReview(clean,card._score);$('#scanStatus').innerHTML='<span class=good>✓ '+esc(clean.name)+' • '+esc(clean.collector)+' aggiunta come risultato plausibile. Rimuovi la carta.</span>';stableFrames=0;lastQuad=null}catch(e){vLocked=true;$('#scanStatus').innerHTML='<span class=bad>'+esc(e.message)+'</span>'}finally{vBusy=false}}
const detector=detectCard;
startDetection=function(){clearInterval(detectTimer);detectTimer=setInterval(()=>{const d=detector();if(d){missing=0;lastDet=d;seen++;if(!vLocked)$('#scanStatus').textContent=seen>=3?'Carta stabile • acquisisco…':'Carta rilevata…';if(rapid&&!vBusy&&!vLocked&&seen>=3)scanDetected({...d,stable:true})}else{missing++;seen=0;if(missing<6&&lastDet){if(!vLocked)$('#scanStatus').textContent='Mantengo aggancio…';return}lastDet=null;if(missing>=6){vLocked=false;document.querySelector('#duplicateBtn')?.remove();lastCode=''}if(!vLocked)$('#scanStatus').textContent='Cerco la carta…'}},220)};
})();